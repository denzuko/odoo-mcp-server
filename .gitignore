# Build output
nob
odoo-mcp
odoo-mcp.wasm
*.o

# Security artefacts — regenerated by CI, not committed
sbom.cdx.json
sbom.spdx.json
osv.sarif.json
cppcheck.sarif.json
kics-tf.sarif.json
ast_calls.json

# Terraform runtime
terraform/.terraform/
terraform/plan.tfplan
terraform/plan.json
terraform/*.tfstate
terraform/*.tfstate.backup

# Cloudflare Workers local dev secrets
workers/.dev.vars

# Editor/OS noise
*.swp
*~
.DS_Store
.pytest_cache/
__pycache__/

# Live env file with real secrets — template only is committed
# (the live copy lives on the server at ~/.config/containers/systemd/odoo-mcp.env)
env/odoo-mcp.env.live
workers/.dev.vars
build/
